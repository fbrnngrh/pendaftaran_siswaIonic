import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pendaftaran_siswa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
